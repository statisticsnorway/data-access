grpcserver:
  port: 0

data-access:
  provider: no.ssb.dapla.data.access.service.LocalstackDataAccessService

tracing:
  enabled: false

metadatads:
  format: PKCS12
  keystore: src/test/resources/metadata-signer_keystore.p12
  keyAlias: dataAccessKeyPair
  password: changeit
  algorithm: SHA256withRSA

routing:
  file: src/test/resources/routing.json