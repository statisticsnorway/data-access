grpcserver:
  port: 0

tracing:
  enabled: false

metadatads:
  signer:
    format: PKCS12
    keystore: src/test/resources/metadata-signer_keystore.p12
    keyAlias: dataAccessKeyPair
    password: changeit
    algorithm: SHA256withRSA
  verifier:
    format: PKCS12
    keystore: src/test/resources/metadata-verifier_keystore.p12
    keyAlias: dataAccessCertificate
    password: changeit
    algorithm: SHA256withRSA
